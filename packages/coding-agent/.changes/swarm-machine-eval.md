- Added the `pr-manager` reference machine to the swarm capability eval: a guarded review/fix loop (entry -> reviewing -> fixing -> monitoring) that re-enters reviewing until the verdict approves, then parks a resident monitoring state; the replay checker accepts the state-machine event kinds (state_entry, transition_fired, transition_blocked, wait_settled) and cross-checks usage.transitions_fired, and the deterministic tests cover the machine spec shape, the ANSWER formats, the fix-ledger/rounds checks, and machine-form replay fixtures.
