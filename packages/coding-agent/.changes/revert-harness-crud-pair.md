- Restored the twelve per-kind continual-harness CRUD methods on `rlm.harness` (`create_memory`, `update_memory`, `delete_memory`, `create_skill`, `update_skill`, `delete_skill`, `create_subagent`, `update_subagent`, `delete_subagent`, `create_prompt_note`, `update_prompt_note`, `delete_prompt_note`) and the `path` spelling of the harness entry grouping, reverting the consolidation and rename from #2154 at the user's request.
- Restored `rlm.harness.record_refinement` and `rlm.harness.plan_refinement` on the model-facing kernel surface, reverting #2155. `rlm.harness.overview()` and `rlm.get_harness_state().refinements` keep reading engine-recorded refinement events.
