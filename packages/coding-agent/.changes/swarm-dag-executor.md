- Added the swarm DAG executor: `rlm.swarm.run/status/stop/resume` validates a stored swarm, spawns ready nodes without holding the model turn open, resumes through collect polls, binds typed outputs into dependent prompts, enforces per-node and run budgets, retries failures, applies fail_fast/continue/escalate policies, cascades cancellation, and wakes the parent once per milestone.
