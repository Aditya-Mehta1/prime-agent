- Removed duplicated Prime HTTP helpers and duplicated daemon socket/guard code: the Prime request helpers now live in one module, the daemon clients share one connect helper, and the response and session-summary wire guards are owned by the daemon protocol module. A failed daemon worker connect now releases its socket, so the next connect attempt can retry, and the agents view now rejects session summaries without a working directory.
